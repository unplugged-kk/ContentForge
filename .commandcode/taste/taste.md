# Taste

- Manages machine/dotfiles configuration declaratively with Nix + home-manager, kept in a git repo (`~/git/dotfiles` with `flake.nix`, `home.nix`, `configuration.nix`, `bootstrap.sh`, `rebuild.sh`). Confidence: 0.6
- Prefers config to live in version-controlled dotfiles (integrated with the existing repo's conventions and applied via the repo's own apply script/procedure) rather than as ad-hoc system files. Confidence: 0.6
